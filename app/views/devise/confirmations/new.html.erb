<div class="flex flex-col justify-center items-center w-full px-4 py-8 space-y-8 lg:space-y-0 lg:space-x-12 z-10">
  <div class="bg-white w-2/3 px-6 py-10 shadow-md rounded-lg">
    <p class="text-2xl mb-4 text-center text-zinc-800">確認メールの再送</p>
    <p class="mt-4 text-zinc-600 text-center">メールが届かない場合は、以下から再送信できます。</p>
    <%= form_with model: resource, url: user_confirmation_path(resource_name), local: true, format: :html, data: { turbo: false } do |f| %>
      <%= render "devise/shared/error_messages", resource: resource %>
      <% if notice %>
        <p class="text-green-600 mt-4"><%= notice %></p>
      <% elsif alert %>
        <p class="text-red-600 mt-4"><%= alert %></p>
      <% end %>

      <div class="mb-4 mt-8">
        <%= f.label :email, "メールアドレス", class: "block mb-1 text-zinc-800" %>
        <%= f.email_field :email, class: "text-zinc-800 w-full px-4 py-2 border border-gray-300 rounded-md focus:outline-none focus:ring-2 focus:ring-blue-500", placeholder: "メールアドバイス" %>
      </div>
      <%= f.submit "認証メールを再送する", class: "w-full bg-sky-600 text-white py-2 rounded-full hover:bg-red-400 hover:text-white transition" %>
    <% end %>
  </div>
</div>
